import java.util.ArrayList;


public class InsertionSort extends Thread {
	
	private int[] a;								// de array die gesorteerd moet worden
	private int n;									// aantal getallen in de array.
	private ArrayList<Integer> gl;					// de gesorteerde lijst
	
	public InsertionSort(int[] a, int n, ArrayList<Integer> gl) {
		this.a = a;
		this.n = n;
		this.gl = gl;
	}

	public void opdracht1(int[] array) {
		for (int i = 1; i < array.length; i++) {
			int getal = array[i];
			int x;
			for (x = i; x > 0 && getal < array[x - 1]; x--) {
				array[x] = array[x - 1];
				gl.set(x, array[x-1]);
			}
			array[x] = getal;
			gl.set(x, getal);
		}
	}
	
}
